<?php include './templates/header.phtml'; ?>

<head>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="css/style.css">
</head>

<div class="container py-5">
    <h2 class="text-center mb-5">Lista de Conciertos</h2>

    <?php if (!empty($conciertos)): ?>
        <div class="row">
            <?php foreach ($conciertos as $concierto): ?>
                <div class="col-12 col-md-6 mb-4 d-flex justify-content-center">
                    <div class="card shadow-lg border-0" style="width: 100%; max-width: 350px; transition: 0.3s;">
                        <a href="conciertos/concierto/<?= $concierto->id_concierto ?>" style="text-decoration: none; color: inherit;">
                            <div class="card-body text-center">
                                <h5 class="card-title">
                                    Concierto <?= $concierto->id_concierto ?>
                                </h5>
                                <p class="card-text mb-1">
                                    <strong> Lugar:</strong> <?= $concierto->Lugar ?>
                                </p>
                                <p class="card-text mb-1">
                                    <strong> Banda:</strong> <?= $concierto->id_banda ?>
                                </p>
                                <p class="card-text mb-1">
                                    <strong> Ciudad:</strong> <?= $concierto->Ciudad ?>
                                </p>
                                <p class="card-text mb-1">
                                    <strong> Fecha:</strong> <?= $concierto->Fecha ?>
                                </p>
                                <p class="card-text">
                                    <strong> Hora:</strong> <?= $concierto->Horario ?>
                                </p>
                            </div>
                        </a>

                        <div class="text-center mt-3">
                            <form action="conciertos/eliminar/<?= $concierto->id_concierto ?>" method="POST" style="display:inline;">
                                <button type="submit" class="btn btn-outline-danger px-3 py-1">Eliminar</button>
                            </form>
                        </div>

                        <div class="text-center mt-2 mb-3">
                            <form action="conciertos/editar/<?= $concierto->id_concierto ?>" method="POST" style="display:inline;">
                                <button type="submit" class="btn btn-outline-light px-3 py-1">Editar</button>
                            </form>
                        </div>
                    </div>
                </div>
            <?php endforeach; ?>
        </div>
    <?php else: ?>
        <div class="alert alert-warning text-center">No hay conciertos cargados aún.</div>
    <?php endif; ?>
</div>

<?php include './templates/footer.phtml'; ?>